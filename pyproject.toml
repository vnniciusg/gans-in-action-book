[project]
name = "gans-in-action-book"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "keras>=3.11.3",
    "matplotlib>=3.10.6",
    "tensorflow>=2.20.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
]
